module.exports = {
    setApiKey() {
        // empty function allows functions to be called during testing
        // without actually sending email or setting the API key
    },
    send() {

    }
}